diff --git i/src/core/message/messages/BindProtocol.cpp w/src/core/message/messages/BindProtocol.cpp
index f4d509a..670ed45 100644
--- i/src/core/message/messages/BindProtocol.cpp
+++ w/src/core/message/messages/BindProtocol.cpp
@@ -67,10 +67,13 @@ CBindProtocolMessage::CBindProtocolMessage(const std::string& protocol, uint32_t
 
     m_data.emplace_back(HW_MESSAGE_MAGIC_TYPE_VARCHAR);
 
-    m_data.append_range(g_messageParser->encodeVarInt(protocol.length()));
-    m_data.append_range(protocol);
+    auto _ar14 = g_messageParser->encodeVarInt(protocol.length());
+    m_data.insert(m_data.end(), _ar14.begin(), _ar14.end());
+    auto _ar15 = protocol;
+    m_data.insert(m_data.end(), _ar15.begin(), _ar15.end());
 
-    m_data.append_range(std::vector<uint8_t>{HW_MESSAGE_MAGIC_TYPE_UINT, 0, 0, 0, 0});
+    auto _ar16 = std::vector<uint8_t>{HW_MESSAGE_MAGIC_TYPE_UINT, 0, 0, 0, 0};
+    m_data.insert(m_data.end(), _ar16.begin(), _ar16.end());
 
     std::memcpy(&m_data[m_data.size() - 4], &version, sizeof(version));
 
diff --git i/src/core/message/messages/FatalProtocolError.cpp w/src/core/message/messages/FatalProtocolError.cpp
index e8976f5..f3baeb9 100644
--- i/src/core/message/messages/FatalProtocolError.cpp
+++ w/src/core/message/messages/FatalProtocolError.cpp
@@ -61,7 +61,9 @@ CFatalErrorMessage::CFatalErrorMessage(SP<IWireObject> obj, uint32_t errorId, co
         std::memcpy(&m_data[2], &obj->m_id, sizeof(obj->m_id));
     std::memcpy(&m_data[7], &errorId, sizeof(errorId));
 
-    m_data.append_range(g_messageParser->encodeVarInt(msg.size()));
-    m_data.append_range(msg);
+    auto _ar12 = g_messageParser->encodeVarInt(msg.size());
+    m_data.insert(m_data.end(), _ar12.begin(), _ar12.end());
+    auto _ar13 = msg;
+    m_data.insert(m_data.end(), _ar13.begin(), _ar13.end());
     m_data.emplace_back(HW_MESSAGE_MAGIC_END);
 }
diff --git i/src/core/message/messages/HandshakeBegin.cpp w/src/core/message/messages/HandshakeBegin.cpp
index 96b7c57..5857af0 100644
--- i/src/core/message/messages/HandshakeBegin.cpp
+++ w/src/core/message/messages/HandshakeBegin.cpp
@@ -58,7 +58,8 @@ CHandshakeBeginMessage::CHandshakeBeginMessage(const std::vector<uint32_t>& vers
         HW_MESSAGE_MAGIC_TYPE_UINT,
     };
 
-    m_data.append_range(g_messageParser->encodeVarInt(versions.size()));
+    auto _ar11 = g_messageParser->encodeVarInt(versions.size());
+    m_data.insert(m_data.end(), _ar11.begin(), _ar11.end());
 
     const size_t HEAD_SIZE = m_data.size();
 
diff --git i/src/core/message/messages/HandshakeProtocols.cpp w/src/core/message/messages/HandshakeProtocols.cpp
index 202d9bc..0ac499f 100644
--- i/src/core/message/messages/HandshakeProtocols.cpp
+++ w/src/core/message/messages/HandshakeProtocols.cpp
@@ -57,11 +57,14 @@ CHandshakeProtocolsMessage::CHandshakeProtocolsMessage(const std::vector<std::st
         HW_MESSAGE_MAGIC_TYPE_VARCHAR,
     };
 
-    m_data.append_range(g_messageParser->encodeVarInt(protocols.size()));
+    auto _ar8 = g_messageParser->encodeVarInt(protocols.size());
+    m_data.insert(m_data.end(), _ar8.begin(), _ar8.end());
 
     for (const auto& p : protocols) {
-        m_data.append_range(g_messageParser->encodeVarInt(p.size()));
-        m_data.append_range(p);
+        auto _ar9 = g_messageParser->encodeVarInt(p.size());
+        m_data.insert(m_data.end(), _ar9.begin(), _ar9.end());
+        auto _ar10 = p;
+        m_data.insert(m_data.end(), _ar10.begin(), _ar10.end());
     }
 
     m_data.emplace_back(HW_MESSAGE_MAGIC_END);
diff --git i/src/core/socket/SocketHelpers.cpp w/src/core/socket/SocketHelpers.cpp
index bf75385..9506d1b 100644
--- i/src/core/socket/SocketHelpers.cpp
+++ w/src/core/socket/SocketHelpers.cpp
@@ -42,7 +42,8 @@ SSocketRawParsedMessage Hyprwire::parseFromFd(const Hyprutils::OS::CFileDescript
         if (sizeWritten < 0)
             return {.bad = true};
 
-        message.data.append_range(std::span<uint8_t>(buffer, sizeWritten));
+        auto _ar7 = std::span<uint8_t>(buffer, sizeWritten);
+        message.data.insert(message.data.end(), _ar7.begin(), _ar7.end());
 
         // check for control
         cmsghdr* recvdCmsg = CMSG_FIRSTHDR(&msg);
diff --git i/src/core/wireObject/IWireObject.cpp w/src/core/wireObject/IWireObject.cpp
index 0a638f8..4f19b31 100644
--- i/src/core/wireObject/IWireObject.cpp
+++ w/src/core/wireObject/IWireObject.cpp
@@ -118,8 +118,10 @@ uint32_t IWireObject::call(uint32_t id, ...) {
             case HW_MESSAGE_MAGIC_TYPE_VARCHAR: {
                 data.emplace_back(HW_MESSAGE_MAGIC_TYPE_VARCHAR);
                 auto str = va_arg(va, const char*);
-                data.append_range(g_messageParser->encodeVarInt(std::string_view(str).size()));
-                data.append_range(std::string_view(str));
+                auto _ar1 = g_messageParser->encodeVarInt(std::string_view(str).size());
+                data.insert(data.end(), _ar1.begin(), _ar1.end());
+                auto _ar2 = std::string_view(str);
+                data.insert(data.end(), _ar2.begin(), _ar2.end());
                 break;
             }
 
@@ -130,7 +132,8 @@ uint32_t IWireObject::call(uint32_t id, ...) {
 
                 auto arrayData = va_arg(va, void*);
                 auto arrayLen  = va_arg(va, uint32_t);
-                data.append_range(g_messageParser->encodeVarInt(arrayLen));
+                auto _ar3 = g_messageParser->encodeVarInt(arrayLen);
+                data.insert(data.end(), _ar3.begin(), _ar3.end());
 
                 switch (arrType) {
                     case HW_MESSAGE_MAGIC_TYPE_UINT:
@@ -153,8 +156,10 @@ uint32_t IWireObject::call(uint32_t id, ...) {
                     case HW_MESSAGE_MAGIC_TYPE_VARCHAR: {
                         for (size_t i = 0; i < arrayLen; ++i) {
                             const char* element = rc<const char**>(arrayData)[i];
-                            data.append_range(g_messageParser->encodeVarInt(std::string_view(element).size()));
-                            data.append_range(std::string_view(element));
+                            auto _ar4 = g_messageParser->encodeVarInt(std::string_view(element).size());
+                            data.insert(data.end(), _ar4.begin(), _ar4.end());
+                            auto _ar5 = std::string_view(element);
+                            data.insert(data.end(), _ar5.begin(), _ar5.end());
                         }
                         break;
                     }
@@ -233,7 +238,8 @@ void IWireObject::called(uint32_t id, const std::span<const uint8_t>& data, cons
     if (!method.returnsType.empty())
         params.emplace_back(HW_MESSAGE_MAGIC_TYPE_SEQ);
 
-    params.append_range(method.params);
+    auto _ar6 = method.params;
+    params.insert(params.end(), _ar6.begin(), _ar6.end());
 
     if (method.since > m_version) {
         const auto MSG = std::format("method {} since {} but has {}", id, method.since, m_version);
