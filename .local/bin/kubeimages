#!/bin/bash

echo $(kubectl config current-context) && KUBE_CTX_BLOCK=0 kubectl get pods --all-namespaces -o jsonpath="{..image}" |tr -s '[[:space:]]' '\n' |sort |uniq -c|grep "docker.io/monifair" | sed 's@.*/@@'